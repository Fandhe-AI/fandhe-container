# scripts

| Name | Description | Path |
| --- | --- | --- |
| Install | Installation, repository configuration, and major version upgrade commands | [install.md](./install.md) |
| VM Commands | QEMU/KVM virtual machine management via `qm` and `qmrestore` | [vm-commands.md](./vm-commands.md) |
| Container Commands | LXC container management via `pct` and template management via `pveam` | [container-commands.md](./container-commands.md) |
| Cluster Commands | Cluster management via `pvecm`, high availability via `ha-manager`, and storage replication via `pvesr` | [cluster-commands.md](./cluster-commands.md) |
| Storage Commands | Storage management via `pvesm`, Ceph cluster management via `pveceph`, and backup/restore via `vzdump` | [storage-commands.md](./storage-commands.md) |
